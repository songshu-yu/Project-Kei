# Bilibili module release input

`official-release-fragment.json` is the PK-130-owned release input for the
serial official-catalog build. The package builder includes program files only;
credentials, UID lists, profile caches, briefing caches and other local state
are never release assets.
