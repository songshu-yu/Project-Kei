# Voice Pack Registry 发布输入

本目录只保存 `voice_pack_registry@1.0.0` 的公开发布元数据，不保存 Release ZIP、
Voice Pack manifest 实例、模型权重、参考音频、本机注册表或本机路径。

确定性资产由 `features.voice_pack_registry.package_builder` 在显式临时/输出目录
生成。PK-000 只有在语音批通过 PK-900 后，才可将片段并入官方 Catalog 并执行
GitHub Release；本任务不发布。
